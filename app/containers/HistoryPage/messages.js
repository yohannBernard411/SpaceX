import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.containers.HistoryPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'History of SpaceX',
  },
});
