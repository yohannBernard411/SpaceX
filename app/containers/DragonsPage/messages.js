import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.containers.DragonsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'All Dragons of SpaceX',
  },
});
